# Write a program to use string.format() method to format the following three variables as per the expected output

totalMoney = 1000
quantity = 3
price = 450

data = "I have {0} dollars so I can buy {1} football for {2} dollars."

print(data.format(totalMoney, quantity, price))